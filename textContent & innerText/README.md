# 👀 textContent vs innerText

A concise **JavaScript DOM practice** project to understand the subtle difference between `textContent` and `innerText`.

---

## ⚡ Concept

- **`textContent`** → Retrieves **all text**, including hidden elements (`display: none`).  
- **`innerText`** → Retrieves **only visible text** as rendered on the page.

💡 Confusing but essential: Hidden text is included in `textContent` but ignored in `innerText`.

---

## 🛠️ Usage

1. Open `index.html` in a browser.  
2. Click **Show textContent** → displays **all text**.  
3. Click **Show innerText** → displays **only visible text**.

---

## 📂 Project Structure

- `index.html` → HTML structure  
- `style.css` → Styling for container & buttons  
- `script.js` → JS logic for button events  

---

## 🎯 Learning Outcomes

- Selecting elements using `querySelector`  
- Handling DOM events with `addEventListener`  
- Understanding **all text vs visible text**  
- Dynamic text updates in the DOM  

---

Made for **hands-on JavaScript practice** to clarify one of the most confusing DOM concepts. ✅
